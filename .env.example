# ===========================================
# DATABASE (Vercel Postgres / Neon / Supabase)
# ===========================================
# Get these from your Vercel Postgres dashboard or Neon/Supabase
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
# Optional: Direct URL for migrations (bypasses connection pooling)
DIRECT_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ===========================================
# MAKE.COM WEBHOOK INTEGRATION
# ===========================================
# URL that receives new order notifications (create in Make.com)
MAKE_WEBHOOK_NEW_ORDER="https://hook.make.com/your-webhook-id-here"

# Secret for authenticating Make.com webhook calls to our API
# Generate with: openssl rand -hex 32
MAKE_WEBHOOK_SECRET="your-secure-random-secret-here"

# ===========================================
# SESSION / AUTH
# ===========================================
# Secret for JWT session signing
# Generate with: openssl rand -hex 32
SESSION_SECRET="your-session-secret-at-least-32-characters-long"

# ===========================================
# APPLICATION
# ===========================================
# Your production URL (set automatically by Vercel, but good to have)
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
